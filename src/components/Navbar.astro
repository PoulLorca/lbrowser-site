---
import { Button } from "@/components/ui/button";

const currentPath = Astro.url.pathname;
const isHomePage = currentPath === "/";
---

<header class="sticky top-0 z-40 w-full border-b bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60">
    <div class="container flex h-16 items-center justify-between px-4">
      <div class="flex items-center">
        <a href="/" class="flex items-center space-x-2">
          <img src="/submarine.svg" alt="Logo" class="h-12 w-12" />
          <span class="inline-block font-bold">LBrowser</span>
        </a>
      </div>

      <nav class="hidden md:flex items-center justify-center flex-1">
        <div class="flex items-center space-x-4">
          <Button variant="ghost" asChild>
            <a href={isHomePage ? "#about" : "/#about"}>About</a>
          </Button>
          <Button variant="ghost" asChild>
            <a href={isHomePage ? "#news" : "/#news"}>News</a>
          </Button>
          <Button variant="ghost" asChild>
            <a href={isHomePage ? "#get-involved" : "/#get-involved"}>Get Involved</a>
          </Button>
          <Button variant="ghost" asChild>
            <a href={isHomePage ? "#sponsors" : "/#sponsors"}>Sponsors</a>
          </Button>
          <Button variant="ghost" asChild>
            <a href={isHomePage ? "#faq" : "/#faq"}>FAQ</a>
          </Button>
          <Button variant="ghost" asChild>
            <a href='/docs/'>Docs</a>
          </Button>
        </div>        
      </nav>

      <div class="hidden md:block">
        <Button variant="default" className="rounded-full">
          <a href={isHomePage ? "#donate" : "/#donate"}>Donate</a>
        </Button>
      </div>
    </div>
  </header>
  