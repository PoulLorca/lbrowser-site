---
import { Card, CardContent } from "@/components/ui/card";
---

<section id="sponsors" class="py-20 bg-muted/50">
  <div class="container mx-auto">
    <h2 class="text-3xl font-bold tracking-tight mb-8">Sponsors</h2>
    <Card>
      <CardContent className="p-6">
        <div class="text-center mb-8">
          <p class="text-muted-foreground">
            Agradecemos a nuestros patrocinadores por su continuo apoyo
          </p>
        </div>
        <div class="grid grid-cols-2 md:grid-cols-4 gap-8 items-center justify-items-center">
          {[1, 2, 3, 4, 5, 6, 7, 8].map((sponsor) => (
            <div class="bg-muted aspect-video w-full max-w-[200px] rounded-lg flex items-center justify-center">
              <span class="text-muted-foreground">Sponsor {sponsor}</span>
            </div>
          ))}
        </div>
      </CardContent>
    </Card>
  </div>
</section>
